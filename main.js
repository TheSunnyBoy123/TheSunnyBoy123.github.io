function setup() {
    createCanvas(windowWidth, windowHeight);
}

function draw() {
    // let button_to_animation = createA("js.html", "Click to see animation", );
    // button_to_animation.position(0, 0);
    background(0, 0, 0);
}